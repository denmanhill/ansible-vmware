---
- hosts: ESXiHosts[0]
  vars:
    wantedcluster: CLUSTER01
    usedmemory: 0
  connection: local
  tasks:
  - name: Grab cluster facts
    vmware_cluster_facts:
      hostname: "{{ vmvcenter }}"
      username: "{{ VCC_USERNAME }}"
      password: "{{ VCC_PASSWORD }}"
      datacenter: "{{ vmdatacenter }}"
      validate_certs: false
    register: cluster_facts_json
  
  - name: Loop with with_dict
    debug: msg={{ item.enabled_drs }}
    with_dict: "{{ cluster_facts_json['clusters'] }}"
    when: item.key == wantedcluster
    loop_control:
      label: "{{ item.enabled_drs }}"
    ignore_errors: yes
  
  - name: Grab resource pool facts
    vmware_resource_pool_facts:
      hostname: "{{ vmvcenter }}"
      username: "{{ VCC_USERNAME }}"
      password: "{{ VCC_PASSWORD }}"
      validate_certs: false
    register: rp_facts
  
  - debug: var=rp_facts.resource_pool_facts
    
  - name: Loop with loop
    set_fact: 
      cpumaxusage: "{{ item.runtime_cpu_max_usage }}"
    loop: "{{ rp_facts.resource_pool_facts }}"
    loop_control:
      label: "{{ item.runtime_cpu_max_usage }}"
    ignore_errors: yes

  - debug: var=cpumaxusage
